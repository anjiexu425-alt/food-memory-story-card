---
name: food-memory-story-card
description: 把美食照片变成可点击翻转的数字美食记忆故事卡（HTML）。自动探测宿主能力：具备视觉理解+图像生成能力的宿主（如 ChatGPT、豆包）用照片生成手帐风正面图并嵌入；能力不足时自动降级（文字描述 + 原图或 inline SVG），绝不假装看图。当用户提供食物照片（本地路径/链接）、用文字描述食物，或提到"美食故事卡""可翻转卡片""互动故事卡"时使用。
whenToUse: 用户想把美食照片做成可以在浏览器里点击翻转的互动记忆卡片时
user-invocable: true
---

# Food Memory Story Card（可翻转数字版）

把一张真实美食照片（或文字描述的食物），变成一张可以在浏览器中点击翻转的数字记忆故事卡。

**面向任意宿主**：多模态宿主（ChatGPT、豆包等）与纯文本宿主均可使用，Skill 会先探测宿主能力，能走主路径就走主路径，能力不足时自动降级，且**绝不假装看过图或生成过图**。

## 能力探测（第一步；不要写死任何工具名）

开始前先确认宿主三项能力（用布尔值表示；如何得知由宿主自己决定：是否有可读取图片的工具/多模态视觉模型、是否有图像生成工具、生成工具是否返回可保存的文件路径或 base64）：

| 能力 | 含义 | 判定示例 |
| --- | --- | --- |
| `can_read_images` | 能否理解图片内容（视觉理解） | 调用图片读取工具成功 / 模型声明支持图片输入 |
| `can_generate_images` | 能否生成图片 | 存在可用的图像生成工具 |
| `can_access_generated_image` | 能否拿到生成图片的可嵌入内容 | 生成工具返回可保存的文件路径或 base64 / data URI |

本仓库提供参考实现：`helpers/capabilities.js`（`detect()` / `resolveRoute()` 决策表，含路由说明）。

### 主路径（三项能力全部可用）

```
照片 → 视觉分析（观察原图）
     → 生成手帐风正面图（图像生成工具）
     → 获取生成图片（文件路径 / base64）
     → 转 data URI 嵌入 HTML
     → 背面记忆故事（只写真实信息）
     → 可翻转单文件 HTML
```

### 降级路径（按缺失能力自动切换；每次都要向用户说明走了哪条）

| 触发场景 | 处理方式 | 禁止事项 |
| --- | --- | --- |
| `can_read_images = false`（无视觉） | 明示无法读图，改用用户文字描述（菜名、食材、颜色、摆盘、氛围）构建画面 | 假装看过照片、编造照片细节 |
| `can_generate_images = false`（无生图） | 有可读取的原图则用原图（转 data URI）；否则 inline SVG / CSS 手绘手帐视觉 | 引用不存在的图片或外部链接 |
| 生图工具不可用 / 调用失败 | 明确告知用户「生图失败」，转原图或 inline SVG | 跳过生图、用占位符冒充生成图 |
| 生图成功但取不到文件（`can_access_generated_image = false`） | 生成图单独交付给用户，HTML 改用原图或 SVG，并明确告知 | 虚报「图片已嵌入」、假路径、假 base64 |

**诚实原则（必须遵守）**：任何降级都要明确告知用户路径与原因，不得静默降级、不得假装能力存在。

## 图片进入 HTML（真实流程，杜绝「HTML 有了图却没显示」）

- 真实图片文件 → `data:image/<type>;base64,...`（只做真实文件读取，参考 `helpers/image-io.js` 的 `fileToDataUri()`）。
- 只有 `data:image/...` 的 data URI 允许嵌入 HTML；**任何假路径、假 base64 一律禁止**，出现即自动降级为原图 / inline SVG / 占位，并告知用户。
- 产物必须单文件、自包含、无外部资源；组装后用 `helpers/html-builder.js` 的 `assertSelfContained()` 复核（无外部 `<script src>` / `<link>` / `url()`）。

## 记忆诚实（不编造事实）

- 背面只填写**用户明确提供**或**照片中客观可识别**的信息。
- 日期、地点、同行人、心情、经历等**未提供 → 一律留空位（________）**，绝不自行编造。
- 故事文案只写可确认内容 + 通用氛围（如「照片里的这顿 brunch，阳光正好」）；用户未讲述经历时，在文案后留故事空位，邀请用户补写自己的回忆。

## 交互流程（先询问输出形式，再按选择执行）

**用户一提出需求（无论说要照片、要卡片，还是只说「帮我做美食故事卡」），先主动询问输出形式**，列出选项让用户选择：

- **A. 只要手帐风格照片**（图片，不生成 HTML）
- **B. 做成可点击翻转的 HTML 记忆卡**
- **C. 两者都要**（先出图，再做卡片）

按用户选择执行：

1. 用户选 **A**（或明确只要照片）→ 只生成手帐风格照片并交付，**不生成 HTML**；
2. 用户选 **B**（或明确要卡片）→ 进入卡片流程：先按「做卡片前先主动询问内容」收集日期 / 地点 / 心情 / 故事，再生成可翻转 HTML；
3. 用户选 **C** → 先交付手帐风格照片，再按卡片流程生成 HTML（照片可嵌入卡片或单独交付，按能力分流）。

询问只做一次；输出形式确定之前，**不要生成最终产物**（图片或 HTML）。若用户已明确指定形式，确认一次即可，不要反复追问。

## 一次性指令模式（照片 + 卡片一步到位）

用户在**同一条消息**里同时说明「输出形式」和「背面内容」时，**不要再询问**，直接全部完成：

- 形式：用户说「两种都要 / 照片和卡片 / 都要」→ 按 C 执行：先出手帐风格照片，再做可翻转 HTML 卡片；
- 内容：从用户话里**自动提取**日期 / 地点 / 一起吃的人 / 心情 / 故事填入背面；没提到的字段留空位，不编造；
- 用户只发照片 + 一句「做照片和卡片」→ 背面全部留空位（可编辑），不询问、不编造。

用户用自然语言表达即可，无需记住固定格式。

## 做卡片前先主动询问内容

用户选择做卡片（B 或 C）后，**先一次性询问**希望在卡片背面添加什么内容，包括：**日期、地点、一起吃的人、心情、想写进卡片的回忆/故事**。用户回答多少用多少，未提供的字段一律留空位，**绝不编造**；若用户已主动提供（如一开始就说了日期、地点、同行人），直接用，不重复询问。不要逐项追问，一次问完；用户表示「随便 / 你定」时，只填照片中可客观识别的信息，其余留空。

## 核心目标

最终输出一个完整的、可直接保存并在浏览器打开的 HTML 文件。该 HTML 包含：
1. 正面：手帐风格插画（或原图/生成图）+ 6 张贴纸 + 3 个英文关键词
2. 背面：真实记忆故事文案 + 可填写空位
3. 点击卡片任意位置即可 3D 翻转

## Workflow

0. **判断意图**：用户只要「手帐风格照片」→ 按「交互流程」先出图并询问是否做卡片；用户明确要卡片 → 直接继续。
1. **探测能力**：确认 `can_read_images` / `can_generate_images` / `can_access_generated_image`（不写死工具名）。
2. **获取食物画面**：能读图则观察照片；不能则向用户索取文字描述，绝不假装看图。
3. 识别主菜、情绪、核心视觉锚点（只基于确认信息）。
4. 生成正面插画描述（手帐风格：暖白纸、哑光水粉/剪纸感、粗粝边缘、低饱和、温暖怀旧）。
5. 选择正好 6 个故事向贴纸元素 + 3 个英文关键词。
6. 撰写背面中文文案（温暖克制；只写真实信息，未提供的字段留空位）。
7. **按能力生成正面视觉**（有生图能力时**必须显式调用宿主提供的图像生成工具**（如 DALL·E、Imagen、豆包生图等）实际生成正面图，禁止跳过生图、禁止用占位符冒充生成图）：
   - 能生图且能取图 → 调用图像生成工具生成手帐风正面图，取图并转 data URI（主路径）；
   - 能生图但取不到图 → 明确告知「生图成功但未嵌入」，生成图单独交付 + HTML 用原图或 SVG，如实说明；
   - 生图工具不可用 / 调用失败 → 明确告知用户，转原图或 inline SVG；
   - 无生图但有可读原图 → 原图转 data URI 作正面；
   - 其余 → inline SVG / CSS 手绘。
8. 组装单文件可翻转 HTML（base64 或 SVG 内联、无外部依赖），用 `assertSelfContained()` 复核；有降级警告则随产物一并说明。

## 正面视觉要求（固定风格规范，必须遵守）

**固定风格：日系复古美食手帐拼贴风（Japanese Vintage Food Scrapbook Journal Style）**

不是普通照片滤镜，也不是单纯插画海报：把真实美食照片转化为一页有**生活记录感**与**记忆感**的手帐页面——暖白纸张纹理、撕纸边缘、和纸胶带、贴纸、手写标签、小涂鸦，保持**低饱和、哑光、温暖、怀旧、轻治愈**，像一本私人生活手帐中的一页，而不是商业广告图或菜单海报。

### 风格关键词
Japanese vintage scrapbook · food journal · memory collage · warm nostalgic · matte gouache illustration · torn paper · washi tape · sticker sheet · handwritten notes · soft low-saturation palette

### 输出目标
1. 看起来像一张**完整的手帐卡片**，而不是单独一张食物插画；
2. **保留原始照片中的核心食物信息与氛围特征**；
3. 形成**统一系列感**：换别的美食照片也明显属于同一视觉系统；
4. 兼具「食物记录」与「生活记忆」属性；
5. 画面精致、温暖、清爽，不杂乱、不过度堆砌元素。

### 固定版式结构（必须遵守）
- 画幅：固定 **3:2 横向构图**
- 版式：**左侧约 2/3 主视觉区 + 右侧约 1/3 贴纸/标签区**
- 左侧主视觉：以原照片为基础手帐化重绘或拼贴呈现，保留食物主体、餐桌氛围、饮品/关键配角；像被贴在纸面上的「照片插画」，可带撕纸边缘、轻微阴影、胶带固定、拍立得/拼贴纸片感
- 右侧贴纸区：固定 **正好 6 个贴纸**，来自原图可提炼内容（主菜/饮品/小食/水果/甜点/小动物/餐厅小细节/餐具/装饰物）；每个贴纸有独立轮廓、白边或剪贴感、轻微阴影，简洁可爱但不过度幼稚
- 关键词：固定 **3 个英文关键词**，偏情绪/氛围/用餐感受（如 Cozy、Brunch、Yummy、Fresh、Happy、Sunny、Sweet、Relaxing），手写感、不过于正式

### 视觉风格规则
1. **纸张与背景**：暖米白/奶油白/旧纸感底色 + 轻微纹理（手帐纸纹、颗粒感、拼贴底纸、轻微泛黄）；禁止纯白数码背景、高对比科技背景、黑底或霓虹
2. **食物表现**：低饱和、哑光、水粉/彩铅/手绘拼贴感，保持真实结构与识别度；禁止过度写实商业修图、高饱和海报风、过度卡通化失真、夸张二次元萌化
3. **装饰元素**（克制、均衡、不过量）：和纸胶带、撕纸边、爱心、星星、小叶子/小花/植物线稿、小火花/小光点、手写短句、纸片标签——装饰只增强「手帐感」，不喧宾夺主

### 固定色彩系统
- 低饱和暖色系：奶油白、米色、焦糖棕、卡其色、橄榄绿、抹茶绿、柔和橙黄、淡粉棕、浅灰褐
- 整体偏暖、饱和度低、明度柔和，不刺眼、不荧光、不强数码感
- 禁止：荧光粉/荧光绿/亮紫等高刺激色、赛博蓝紫霓虹风、高饱和儿童海报风

### 内容提取规则
- 优先提取：主菜/核心食物、饮品、配菜/甜点/水果、餐桌氛围、有趣小细节（餐厅 logo、小动物、餐具、特色杯子、菜单边角、桌布花纹）
- 只挑最有记忆点的元素做「主图」和「贴纸」，不机械堆砌；**贴纸固定 6 个，不多不少**

### 文案风格规则
- 正面只放少量文字：3 个英文关键词 + 1–2 个短手写词 + 可选一句短句
- 气质：温柔、日常、有生活气息、像手帐随手记录、不营销化
- 示例：Cozy / Brunch Time / Good food, good mood / Yummy / Sweet afternoon / Happy bites

### 一致性要求
- 系列生成时保持统一：3:2 画幅、左主图+右贴纸结构、暖白纸张、低饱和复古色调、手帐拼贴语言、装饰密度、文字风格、贴纸 6 个、关键词 3 个
- 允许变化：主体食物、贴纸对象、关键词内容、局部小装饰、纸胶带位置、小短句

### 禁止跑偏项（必须遵守）
- 不要变成普通插画海报 / 菜单设计 / 电商宣传图
- 不要赛博风 / 科技风 / 高饱和儿童卡通风
- 不要极简到没有手帐感；不要堆太多元素导致拥挤
- 不要省略右侧贴纸区；贴纸不要超过或少于 6 个；不要省略 3 个英文关键词
- 不要让整体失去「温暖、怀旧、私人生活记录」的核心气质

## 背面文案要求
- 中文，2-4 句，温暖有画面感
- 必须包含可填写空位：日期、地点、一起吃的人、心情（未提供则留空）
- 风格像手帐里随手写的话，不要煽情，**不要编造未提供的事实**

## HTML 技术要求（必须严格遵守）
- 输出一个完整的、单文件 HTML（包含 CSS 和必要的 JS）
- 使用 CSS 3D transform 实现翻转效果
- 点击卡片任意位置即可翻转（再次点击翻回）
- 卡片居中显示，有轻微悬浮阴影
- 适配手机和电脑屏幕
- 不要依赖外部图片/CSS/JS（正面图用 data URI 或 inline SVG 内联）
- **背面可编辑**：日期、地点、一起吃的人、心情、故事等字段用 contenteditable 实现，用户可在浏览器里点击直接编辑；点击编辑区时**不得触发翻转**；空位用 CSS 占位符（:empty::before）显示，点击即消失可直接填写
- 代码干净，注释清晰（含图片替换说明注释）

## 输出格式
1. 先简要说明：**本次走主路径还是哪条降级路径（及原因）**、核心锚点、6 张贴纸内容、3 个关键词、故事文案（注明哪些是留空位）
2. 然后输出完整的 HTML 代码（用代码块包裹）
3. 告诉用户：把代码保存为 `美食记忆故事卡.html`，用浏览器打开即可点击翻转
4. 最后给出 2-3 个互动选项（例如：换故事风格、生成透明贴纸页、做系列卡片等）
