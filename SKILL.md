---
name: food-memory-story-card
description: 把美食照片变成可点击翻转的数字美食记忆故事卡（HTML）。生成一张支持3D翻转的手帐风格卡片，正面是插画+贴纸，背面是故事文案。当用户提供食物照片（本地文件路径或图片链接）、用文字描述食物，或提到"美食故事卡""可翻转卡片""互动故事卡"时使用。
whenToUse: 用户想把美食照片做成可以在浏览器里点击翻转的互动记忆卡片时
user-invocable: true
---

# Food Memory Story Card（可翻转数字版）

把用户提供的美食照片（或文字描述的食物），变成一张可以在浏览器中点击翻转的数字记忆故事卡。

## 能力分流（Capability Routing）

产品主目标不变：在具备视觉理解 + 图像生成能力的宿主（如 ChatGPT、豆包）中，用户上传美食照片后，直接得到「手帐风正面图 + 背面记忆故事 + 可翻转 HTML」。

**开始任何工作前，先探测宿主的三项能力**，据此走主路径或自动降级：

| 能力开关 | 含义 | 如何探测 |
| --- | --- | --- |
| `can_read_images` | 宿主能否理解图片内容（视觉理解） | 尝试用 read_image 读取用户图片；若模型不支持图片输入（报错 "model does not declare image input"）则为 false |
| `can_generate_images` | 宿主能否生成图片 | 是否存在可用的图像生成工具 |
| `can_access_generated_image` | 能否拿到生成图片的可嵌入内容 | 生成工具是否返回可保存的图片文件路径或 base64 / data URI |

### 主路径（三项能力全部可用）

```
照片 → 视觉分析（read_image 观察原图）
     → 生成手帐风正面图（图像生成工具）
     → 获取生成图片（文件路径 / base64）
     → base64 嵌入 HTML
     → 背面故事文案
     → 可翻转 HTML
```

### 降级路径（能力不足时自动切换）

| 触发场景 | 降级策略 | 禁止事项 |
| --- | --- | --- |
| `can_read_images = false`（无视觉） | 明示无法读图，改用用户文字描述（菜名、食材、颜色、摆盘、氛围）构建画面 | 禁止假装看过照片、禁止编造照片细节 |
| `can_generate_images = false`（无生图） | 不用位图，改用 inline SVG / CSS 手绘手帐视觉（剪纸扁平风、暖白纸、低饱和） | 禁止引用不存在的图片或外部链接 |
| `can_generate_images = true` 但 `can_access_generated_image = false`（生图成功但取不到文件） | 改用 SVG fallback；或把生成图片与 HTML 分开交付（图片存为独立文件，HTML 用相对路径引用或留替换位）并明确告知 | 禁止虚报「图片已嵌入」，禁止假 base64 |

**诚实原则（必须遵守）**：任何降级都要向用户明确说明当前走的是哪条路径、为什么，不得静默降级、不得假装能力存在。

## 输入方式

按 `can_read_images` 决定取画面方式。对于 DSH 等无图片上传界面的宿主：不要索要上传，直接按文字描述生成。

1. `can_read_images = true` 且用户提供本地图片路径（如 `~/Desktop/拉面.jpg`）→ 用 read_image 读取并仔细观察；
2. `can_read_images = true` 且用户提供图片 URL → 尝试获取后观察；
3. `can_read_images = false` → 不读图、不假装看图，直接向用户索取文字描述；若用户已给出图片路径，如实说明当前模型读不了图，请其改用文字描述。

## 核心目标
最终输出一个完整的、可直接保存并在浏览器打开的 HTML 文件。
该 HTML 包含：
1. 正面：手帐风格插画 + 6张贴纸 + 3个英文关键词
2. 背面：温暖中文故事文案 + 可填写空位
3. 点击卡片任意位置即可 3D 翻转

## Workflow

1. **探测能力**：确认 `can_read_images` / `can_generate_images` / `can_access_generated_image` 三项状态。
2. **获取食物画面**：按「输入方式」——能读图则 read_image 观察；不能则用用户文字描述，绝不假装看图。
3. 识别主菜、情绪、核心视觉锚点。
4. 生成正面插画描述（手帐风格：暖白纸、哑光水粉/剪纸感、粗粝边缘、低饱和、温暖怀旧）。
5. 选择正好 6 个故事向贴纸元素 + 3 个英文关键词。
6. 撰写背面中文故事文案（60-100字，温暖克制，留填写空位）。
7. **按能力生成正面视觉**：
   - 能生图且能取图 → 生成手帐风正面图并拿到文件 / base64（主路径）；
   - 不能生图 → inline SVG / CSS 手绘插画（SVG fallback）；
   - 能生图但取不到图 → SVG fallback，或把图片文件与 HTML 分开交付并明确说明。
8. 组装可翻转 HTML：正面视觉 + 背面文案 + 3D 翻转，base64 或 SVG 内联，自包含、无外部依赖。

## 正面视觉要求
- 3:2 横向构图
- 左侧大插画，右侧 6 张贴纸
- 暖白纸张纹理、手撕边缘感、轻微阴影
- 禁止写实、禁止光滑3D、禁止过度细节
- SVG fallback 同样遵循以上风格（剪纸扁平色块、低饱和）

## 背面文案要求
- 中文，2-4 句，温暖有画面感
- 必须包含可填写空位：日期、一起吃的人、心情
- 风格像手帐里随手写的话，不要煽情

## HTML 技术要求（必须严格遵守）
- 输出一个完整的、单文件 HTML（包含 CSS 和必要的 JS）
- 使用 CSS 3D transform 实现翻转效果
- 点击卡片任意位置即可翻转（再次点击翻回）
- 卡片居中显示，有轻微悬浮阴影
- 适配手机和电脑屏幕
- 不要依赖外部图片链接（正面图：主路径用 base64 嵌入；fallback 用 inline SVG；分开交付时用相对路径引用并在 HTML 注释里说明）
- 代码干净，注释清晰（含图片替换说明注释）

## 输出格式
1. 先简要说明：**本次走主路径还是哪条降级路径（及原因）**、核心锚点、6张贴纸内容、3个关键词、故事文案
2. 然后输出完整的 HTML 代码（用代码块包裹）
3. 告诉用户：把代码保存为 `美食记忆故事卡.html`，用浏览器打开即可点击翻转
4. 最后给出 2-3 个互动选项（例如：换故事风格、生成透明贴纸页、做系列卡片等）
